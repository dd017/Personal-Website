<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Devansh Deepak</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Raleway:wght@400;600;800&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;500;700&display=swap');

        body {
            margin: 0;
            font-family: 'Raleway', sans-serif;
            background: linear-gradient(135deg, #2e2e2e, #4a4a4a);
            color: #e0e0e0;
            overflow-x: hidden;
            padding-top: 60px; /* For fixed header */
        }

        header {
            background-color: #1a1a1a;
            padding: 1rem 0;
            text-align: center;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.4);
            transition: background-color 0.3s;
        }

        header h1 {
            font-family: 'Orbitron', sans-serif;
            font-size: 2.8rem;
            margin: 0;
            color: #4fd1c5;
            letter-spacing: 0.1rem;
            animation: glow 1.5s infinite alternate;
        }

        nav ul {
            list-style: none;
            padding: 0;
            display: flex;
            justify-content: center;
            margin: 0;
        }

        nav ul li {
            margin: 0 1.5rem;
        }

        nav ul li a {
            color: #4fd1c5;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s, transform 0.3s;
            padding: 0.5rem;
        }

        nav ul li a:hover {
            color: #e0e0e0;
            transform: translateY(-2px);
        }

        .section {
            padding: 4rem 2rem;
            text-align: center;
            animation: fadeIn 1s ease-in-out;
            position: relative;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
        }

        h2 {
            font-size: 2.8rem;
            margin: 1.5rem 0;
            color: #4fd1c5;
            position: relative;
            display: inline-block;
        }

        h2::after {
            content: '';
            display: block;
            width: 60%;
            height: 4px;
            background-color: #4fd1c5;
            margin: 0.5rem auto;
            border-radius: 2px;
        }

        p {
            margin: 1.5rem 0;
            font-size: 1.25rem;
            line-height: 1.7;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
        }

        .card {
            background-color: #333;
            padding: 1.5rem;
            border-radius: 10px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .card img {
            max-width: 100%;
            border-radius: 10px;
            margin-bottom: 1rem;
            transition: transform 0.3s;
        }

        .card img:hover {
            transform: scale(1.05);
        }

        .card p {
            margin: 0;
            font-size: 1.4rem;
            color: #4fd1c5;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.4);
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
        }

        input, textarea, button {
            padding: 1rem;
            border: none;
            border-radius: 8px;
            font-size: 1.1rem;
            background-color: #444;
            color: #e0e0e0;
            transition: background-color 0.3s, border-color 0.3s;
        }

        input:focus, textarea:focus {
            background-color: #555;
            border-color: #4fd1c5;
        }

        button {
            background-color: #4fd1c5;
            color: #ffffff;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
            border-radius: 8px;
        }

        button:hover {
            background-color: #3baaa9;
            transform: scale(1.05);
        }

        footer {
            background-color: #1a1a1a;
            text-align: center;
            padding: 1rem;
            color: #e0e0e0;
            position: fixed;
            width: 100%;
            bottom: 0;
        }

        .container img {
            width: 180px;
            height: 180px;
            border-radius: 50%;
            object-fit: cover;
            margin-bottom: 1rem;
            border: 4px solid #4fd1c5;
            transition: transform 0.3s;
        }

        .container img:hover {
            transform: scale(1.05);
        }

        @keyframes glow {
            from {
                text-shadow: 0 0 12px #4fd1c5, 0 0 24px #4fd1c5, 0 0 36px #4fd1c5, 0 0 48px #4fd1c5;
            }
            to {
                text-shadow: 0 0 24px #6cf2d1, 0 0 36px #6cf2d1, 0 0 48px #6cf2d1, 0 0 60px #6cf2d1;
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const form = document.getElementById("contact-form");
            form.addEventListener("submit", function(event) {
                event.preventDefault();
                alert("Message sent successfully!");
                form.reset();
            });
        });
    </script>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#portfolio">Portfolio</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
        <h1>Devansh Deepak</h1>
    </header>

    <section id="home" class="section">
        <div class="container">
            <img src="profile.jpg" alt="Devansh Deepak">
            <h2>Welcome to Devansh Deepak's World</h2>
            <p>Explore my journey, my projects, and connect with me. Dive into a world of creativity and innovation!</p>
        </div>
    </section>

    <section id="about" class="section">
        <div class="container">
            <h2>About Me</h2>
            <p>Hello! I'm Devansh Deepak, a passionate web developer dedicated to creating engaging and functional websites. With a love for technology and design, I strive to bring unique ideas to life through my work.</p>
        </div>
    </section>

    <section id="portfolio" class="section">
        <div class="container">
            <h2>Portfolio</h2>
            <div class="grid">
                <div class="card">
                    <img src="project1.jpg" alt="Project 1">
                    <p>Project 1</p>
                </div>
                <div class="card">
                    <img src="project2.jpg" alt="Project 2">
                    <p>Project 2</p>
                </div>
                <div class="card">
                    <img src="project3.jpg" alt="Project 3">
                    <p>Project 3</p>
                </div>
            </div>
        </div>
    </section>

    <section id="contact" class="section">
        <div class="container">
            <h2>Contact Me</h2>
            <form id="contact-form">
                <input type="text" id="name" placeholder="Name" required>
                <input type="email" id="email" placeholder="
